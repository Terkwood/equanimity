use crate::*;

fn draw(v: &[MoodReading]) -> String {
    let by_day = group_by_day(v);
    
    panic!("build string");
}

fn group_by_day(v: &[MoodReading]) -> Vec<MoodReading> {
    panic!("group by day");
}